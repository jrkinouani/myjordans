<%= simple_form_for(@basket, :html => { :multipart => true }) do |f| %>
	<%= f.input :name %>
	<%= f.input :description %>
	<%= f.input :size %>
	<%= f.input :price_per_day %>
	<%= f.input :image, as: :file, :multiple => true, name: "post_attachments[image][]" %>
	<%= f.submit %>
<% end%>